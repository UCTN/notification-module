package com.deneme.mail.repository;

import com.deneme.mail.entity.Makine;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MakineRepository extends JpaRepository<Makine, Long> {
}
